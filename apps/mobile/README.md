# BookTalk Mobile (예정)

웹(apps/web) MVP 완성 후 착수합니다.

착수 시 아래 명령으로 초기화 예정:
```bash
npx create-expo-app@latest . --template
```

초기화 후 `packages/ui`, `packages/api-client`를 의존성으로 연결해서
웹과 동일한 컴포넌트/API 클라이언트를 재사용합니다.
